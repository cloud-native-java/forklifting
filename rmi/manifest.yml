---
applications:
- name: rmi
  memory: 512M
  instances: 1
  host: rmi-${random-word}
  path: target/bootiful-forklifted-application-rmi.jar
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"
