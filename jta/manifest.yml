---
applications:
- name: rmi
  memory: 512M
  instances: 1
  host: jta-${random-word}
  path: target/bootiful-forklifted-application-jta.jar
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"
